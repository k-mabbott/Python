SELECT r.*, u.first_name FROM recipes r JOIN users u ON r.user_id = u.id;

SELECT * FROM recipes;

INSERT INTO recipes (user_id, name, description, instructions, date_cooked, under)
VALUES (1,'Test', 'This is a test', 'None needed this is just a test', 2023-05-14, 'yes' );

SELECT r.*, u.first_name FROM recipes r 
JOIN users u ON r.user_id = u.id;


SELECT r.*, u.first_name FROM recipes r 
JOIN users u ON r.user_id = u.id
WHERE r.id = 1;

SELECT * FROM friendships f 
JOIN users u ON u.id = f.user_id 
JOIN users ON friend_id = users.id;

SELECT * FROM friendships f 
JOIN users u ON u.id = f.user_id 
JOIN friendships ON friend_id = users.id;

SELECT * FROM friendships;

SELECT a.first_name as first_name1, b.first_name as first_name2
FROM users a, users b WHERE a.id != b.id
ORDER BY a.first_name, b.first_name
;




